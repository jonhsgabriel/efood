import styled from "styled-components";
import { cores } from "../../styles";

export const HeaderBar = styled.header`
  background-color: ${cores.berje};
  padding: 63px;
  align-items: center;
  justify-content: center;
`